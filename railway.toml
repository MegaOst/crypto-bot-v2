[build]
builder = "nixpacks"

[deploy]
startCommand = ". /opt/venv/bin/activate && gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 120 web.app:app"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10